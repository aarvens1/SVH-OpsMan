// Credentials are loaded from the environment.
// In production (WSL): export vars in ~/.bashrc or set them per-session.
// In development: copy .env.example to .env and fill in values — dotenv
// (imported via "dotenv/config" in index.ts) will pick them up automatically.
export {};
